# cnn-image-classification-cifar10
This project implements an image classification system using a Convolutional Neural Network (CNN) trained on the CIFAR-10 dataset. The model is built using TensorFlow and Keras and classifies images into 10 different categories such as airplane, car, dog, cat, etc.
